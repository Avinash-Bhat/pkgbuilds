# Maintainer: Linus Sjögren <thelinx@unreliablepollution.net>
pkgname=lua-penlight-git
pkgver=20120707
pkgrel=1
pkgdesc="A set of pure Lua libraries focusing on input data handling, functional programming, and OS path management."
arch="any"
url="https://github.com/stevedonovan/Penlight"
license=('MIT')
provides=('lua-penlight')
conflicts=('lua-penlight')
depends=('lua' 'luafilesystem')
makedepends=('git')

_gitroot="$url.git"
_gitname="Penlight"

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [ -d $_gitname ] ; then
    cd $_gitname && git pull origin
    msg "The local files are updated."
  else
    git clone $_gitroot $_gitname
  fi

  msg "GIT checkout done or server timeout"
  msg "Installing..."
} 

package() {
  cd "$srcdir/$_gitname"
	mkdir -p $pkgdir/usr/share/lua/5.1/pl/platf
	install -Dm0664 lua/pl/platf/* \
		$pkgdir/usr/share/lua/5.1/pl/platf
	install -Dm0664 lua/pl/*.lua \
		$pkgdir/usr/share/lua/5.1/pl/
	install -Dm0664 LICENCE.txt \
		$pkgdir/usr/share/licenses/$pkgname/LICENCE.txt
}
